# 🔗 نظام ربط البيانات الديناميكي
## Dynamic Data Binding System

---

## 📋 نظرة عامة

تم إضافة نظام ربط بيانات ديناميكي متقدم يربط البيانات من ملف `config.js` مع جميع صفحات الموقع تلقائياً.

---

## ✨ المميزات

### 1. **ربط تلقائي للبيانات**
- يتم تحميل البيانات من `src/config.js` تلقائياً عند فتح أي صفحة
- لا حاجة لكتابة HTML يدوياً للجداول والبطاقات
- تحديث البيانات في مكان واحد يحدثها في كل الصفحات

### 2. **اكتشاف تلقائي للصفحة**
- يكتشف النظام الصفحة الحالية تلقائياً
- يحمل البيانات المناسبة لكل صفحة

### 3. **تحديث تلقائي**
- تحديث البيانات كل 30 ثانية تلقائياً
- إمكانية التحديث اليدوي

---

## 🗂️ الملفات المضافة

```
src/
  ├── config.js          # ملف البيانات الرئيسي
  └── data-binding.js    # نظام ربط البيانات (جديد)
```

---

## 🚀 كيفية الاستخدام

### 1. **إضافة البيانات في config.js**

```javascript
const DashboardConfig = {
    tasks: [
        {
            name: 'مهمة جديدة',
            project: 'مشروع اختباري',
            assignee: 'محمد أحمد',
            status: 'active',
            progress: 50,
            dueDate: '2026-03-15'
        }
    ]
};
```

### 2. **إضافة data-bind في HTML**

```html
<tbody data-bind="tasks-tbody">
    <!-- سيتم ملء البيانات تلقائياً -->
</tbody>
```

### 3. **النتيجة**
سيتم ملء الجدول تلقائياً بالبيانات من config.js مع التنسيق الكامل!

---

## 📊 الصفحات المدعومة

| الصفحة | data-bind | البيانات من config.js |
|--------|-----------|----------------------|
| **index.html** | `dashboard-tasks-tbody` | `config.tasks` (أول 5) |
| **tasks.html** | `tasks-tbody` | `config.tasks` (الكل) |
| **goals.html** | `goals-tbody` | `config.goals` |
| **projects.html** | `projects-tbody` | `config.projects` |
| **performance.html** | `quality-percentage` | `config.charts.quality` |

---

## 🎨 البيانات المدعومة

### 1. **المهام (Tasks)**
```javascript
{
    name: 'اسم المهمة',
    project: 'اسم المشروع',
    assignee: 'المسؤول',
    status: 'active|progress|done|overdue|blocked',
    progress: 75,  // نسبة الإنجاز
    dueDate: '2026-02-28'
}
```

### 2. **الأهداف (Goals)**
```javascript
{
    name: 'اسم الهدف',
    duration: 'ربع سنة',
    owner: 'المالك',
    progress: 85,
    status: 'active|progress',
    dueDate: '2026-03-31'
}
```

### 3. **المشاريع (Projects)**
```javascript
{
    name: 'اسم المشروع',
    team: 'أسماء الفريق',
    owner: 'مدير المشروع',
    progress: 65,
    status: 'active|progress',
    dueDate: '2026-04-30'
}
```

---

## 🔧 API المتاحة

### تحديث البيانات يدوياً
```javascript
window.dataBinding.refresh();
```

### الحصول على بيانات محددة
```javascript
const tasks = window.dataBinding.getData('tasks');
const quality = window.dataBinding.getData('charts.quality.percentage');
```

### تحديث بيانات محددة
```javascript
window.dataBinding.updateData('tasks.0.progress', 90);
```

---

## 🎯 حالات الحالة (Status)

| الحالة | الكلاس | اللون |
|--------|--------|-------|
| **نشط** | `badge-active` | أخضر |
| **قيد التنفيذ** | `badge-progress` | أزرق |
| **مكتملة** | `badge-done` | أخضر داكن |
| **متأخر** | `badge-overdue` | برتقالي |
| **مسدود** | `badge-blocked` | أحمر |

---

## 📝 ملاحظات مهمة

1. **ترتيب السكريبتات مهم جداً:**
```html
<script src="src/config.js"></script>        <!-- أولاً -->
<script src="src/data-binding.js"></script>  <!-- ثانياً -->
<script src="src/auth.js"></script>          <!-- ثالثاً -->
<script src="src/script.js"></script>        <!-- رابعاً -->
```

2. **التحديث التلقائي:**
   - يحدث كل 30 ثانية
   - يظهر في Console: `🔄 تم تحديث البيانات تلقائياً`

3. **التنسيق التلقائي:**
   - التواريخ تُحول إلى نصوص عربية (مثل: "بعد 5 أيام")
   - الأرقام الكبيرة تُحول (مثل: 1500 → 1.5k)
   - شريط التقدم يُضاف تلقائياً

---

## 🌟 مثال كامل

### في config.js:
```javascript
tasks: [
    {
        name: 'تطوير الواجهة',
        project: 'نظام الإدارة',
        assignee: 'أحمد محمد',
        status: 'progress',
        progress: 75,
        dueDate: '2026-03-01'
    }
]
```

### في HTML:
```html
<tbody data-bind="tasks-tbody"></tbody>
```

### النتيجة:
```html
<tr>
    <td>
        <strong>تطوير الواجهة</strong>
        <div class="task-project">نظام الإدارة</div>
    </td>
    <td>أحمد محمد</td>
    <td><span class="badge badge-progress">قيد التنفيذ</span></td>
    <td>
        <div class="progress-container">
            <div class="progress-bar" style="width: 75%"></div>
            <span class="progress-text">75%</span>
        </div>
    </td>
    <td class="task-date">بعد 3 أيام</td>
</tr>
```

---

## ✅ الفوائد

1. ✨ **سهولة الصيانة** - تحديث البيانات من مكان واحد
2. 🚀 **سرعة التطوير** - لا حاجة لكتابة HTML متكرر
3. 🎯 **دقة عالية** - تنسيق موحد تلقائياً
4. 🔄 **تحديث مباشر** - البيانات تتحدث تلقائياً
5. 📱 **قابل للتوسع** - سهل إضافة بيانات جديدة

---

## 🔮 التطوير المستقبلي

- [ ] ربط مع API حقيقي
- [ ] تصفية وبحث ديناميكي
- [ ] ترتيب الجداول
- [ ] تصدير البيانات (Excel/PDF)
- [ ] إشعارات فورية عند تحديث البيانات

---

**تم التطوير بواسطة:** تركي عبدالله  
**التاريخ:** 28 فبراير 2026  
**الإصدار:** 1.0.0
